# Dify 二开本地部署指南

本文记录本仓库用于二次开发测试的本地部署方案。目标是：中间件走 Docker，后端、worker、前端走本地源码，方便改代码、看日志和调试。

验证版本：Dify `1.14.2`  
验证日期：2026-06-05  
验证系统：Windows + Docker Desktop

## 推荐架构

不要用纯 Docker 一键部署做二开。纯 Docker 适合体验产品，但改后端或前端代码后要反复重建镜像，不方便。

推荐模式：

- Docker Compose 只启动中间件：PostgreSQL、Redis、Weaviate、sandbox、plugin-daemon、ssrf_proxy
- `api` 本地源码启动 Flask API
- `api` 本地源码启动 Celery worker
- `web` 本地源码启动 Next.js 前端

访问入口：

- 前端：http://localhost:3000
- API 健康检查：http://localhost:5001/health

## 本机当前状态与日常操作

本机部署目录：

```text
D:\demo1\dify
```

当前开发分支：

```text
local-dev-1.14.2
```

当前已验证状态：

- Dify 本地前端：http://localhost:3000
- 首次访问会跳到 `/install`
- 页面已验证显示 `Setting up an admin account`
- API 健康检查通过：http://localhost:5001/health
- API 返回版本：`1.14.2`
- worker 已连接 Redis 并 ready
- 中间件容器已运行：PostgreSQL、Redis、Weaviate、sandbox、plugin-daemon、ssrf_proxy
- 为避开本机已有 Redis，Dify Redis 宿主机端口使用 `6380`
- 本地源码方案未启动 Socket.IO 协同服务，`api/.env` 已设置 `ENABLE_COLLABORATION_MODE=false`
- 未改动本机已有的 `coze-*` 容器

本机日志目录：

```text
D:\demo1\dify\.codex-logs
```

本机配置文件：

```text
D:\demo1\dify\api\.env
D:\demo1\dify\web\.env.local
D:\demo1\dify\docker\middleware.env
```

日常重启建议分别开 4 个 PowerShell 终端。

终端 1：启动中间件。

```powershell
cd D:\demo1\dify\docker
docker compose --env-file middleware.env -f docker-compose.middleware.yaml -p dify up -d
```

终端 2：启动 API。

```powershell
cd D:\demo1\dify\api

$env:LOG_OUTPUT_FORMAT='text'
$env:LOG_FORMAT='%(asctime)s,%(msecs)d %(levelname)-2s [%(filename)s:%(lineno)d] %(req_id)s %(message)s'
$env:ENABLE_COLLABORATION_MODE='false'

uv run flask db upgrade
uv run flask run --host 0.0.0.0 --port=5001 --debug
```

终端 3：启动 worker。

```powershell
cd D:\demo1\dify\api

$env:LOG_OUTPUT_FORMAT='text'
$env:LOG_FORMAT='%(asctime)s,%(msecs)d %(levelname)-2s [%(filename)s:%(lineno)d] %(req_id)s %(message)s'
$env:EDITION='SELF_HOSTED'

uv run celery -A app.celery worker -P solo -c 1 --loglevel INFO
```

终端 4：启动前端。

```powershell
cd D:\demo1\dify

fnm use 22
$env:NODE_OPTIONS='--max-old-space-size=8192'
$env:NEXT_TELEMETRY_DISABLED='1'

pnpm --dir web run dev -- --hostname 0.0.0.0 --port 3000
```

本机已遇到并处理过的关键坑：

- 宿主机全局环境变量 `LOG_FORMAT=json` 会覆盖 Dify 配置，导致 API 启动失败；启动 API 和 worker 时需要显式设置 `LOG_OUTPUT_FORMAT=text` 和 `LOG_FORMAT=...`。
- Dify 前端默认 Turbopack 首编译在 Windows 上可能内存不足；启动前端时使用 `NODE_OPTIONS=--max-old-space-size=8192`。
- 本机已有 Redis 占用 `6379`；Dify Redis 宿主机端口使用 `6380`。
- 本地源码方案没有启动 Socket.IO 协同服务；`ENABLE_COLLABORATION_MODE` 需要设为 `false`，否则工作流页会反复报 `websocket error`。
- Docker Desktop engine 停止时，中间件会不可用；先启动 Docker Desktop，再执行 compose up。

## 前置工具

需要先安装：

- Git
- Docker Desktop
- `uv`
- `fnm`
- Node.js 22
- `pnpm` 11.1.1

建议命令：

```powershell
fnm install 22
fnm use 22
corepack enable
corepack prepare pnpm@11.1.1 --activate

node --version
pnpm --version
uv --version
docker compose version
```

本项目要求 Node `^22.22.1`。如果看到类似 `Requested version v22.x.x is not currently installed`，先执行：

```powershell
fnm install 22
fnm use 22
```

## 克隆代码

从 fork 的二开分支克隆：

```powershell
git clone -b local-dev-1.14.2 https://github.com/beyondchenlin/dify.git D:\demo1\dify
cd D:\demo1\dify
```

也可以从官方 tag 开始：

```powershell
git clone --branch 1.14.2 --depth 1 https://github.com/langgenius/dify.git D:\demo1\dify
cd D:\demo1\dify
git switch -c local-dev-1.14.2
```

## 生成本地环境文件

```powershell
cd D:\demo1\dify

Copy-Item api\.env.example api\.env
Copy-Item web\.env.example web\.env.local
Copy-Item docker\envs\middleware.env.example docker\middleware.env
```

生成 `api/.env` 的 `SECRET_KEY`：

```powershell
$bytes = New-Object byte[] 42
$rng = [System.Security.Cryptography.RandomNumberGenerator]::Create()
$rng.GetBytes($bytes)
$rng.Dispose()
$secret = [Convert]::ToBase64String($bytes)
$path = 'api\.env'
$content = Get-Content -Raw $path
$content = $content -replace '(?m)^SECRET_KEY=.*$', "SECRET_KEY=$secret"
$utf8NoBom = New-Object System.Text.UTF8Encoding($false)
[System.IO.File]::WriteAllText((Resolve-Path $path), $content, $utf8NoBom)
```

## 端口规划

默认端口：

- Web：`3000`
- API：`5001`
- PostgreSQL：`5432`
- Redis：容器内 `6379`，宿主机建议暴露 `6380`
- Weaviate HTTP：`8080`
- Weaviate gRPC：`50051`
- sandbox：`8194`
- ssrf_proxy：`3128`
- plugin-daemon：`5002`
- plugin debugging：`5003`

如果本机已有 Redis 占用 `6379`，把 Dify Redis 暴露到宿主机 `6380`。

修改 `api/.env`：

```env
REDIS_PORT=6380
CELERY_BROKER_URL=redis://:difyai123456@localhost:6380/1
SSRF_PROXY_HTTP_URL=http://127.0.0.1:3128
SSRF_PROXY_HTTPS_URL=http://127.0.0.1:3128
ENABLE_COLLABORATION_MODE=false
```

修改 `docker/middleware.env`：

```env
EXPOSE_REDIS_PORT=6380
```

注意：Docker 内部服务仍然使用 `redis:6379`，只有宿主机访问 Redis 时用 `localhost:6380`。

## 启动中间件

```powershell
cd D:\demo1\dify\docker
docker compose --env-file middleware.env -f docker-compose.middleware.yaml -p dify up -d
```

检查：

```powershell
docker compose --env-file middleware.env -f docker-compose.middleware.yaml -p dify ps
docker inspect --format "{{.Name}} {{.State.Status}} {{if .State.Health}}{{.State.Health.Status}}{{else}}no-health{{end}}" dify-db_postgres-1 dify-redis-1 dify-sandbox-1 dify-weaviate-1 dify-plugin_daemon-1 dify-ssrf_proxy-1
```

Weaviate 检查：

```powershell
Invoke-WebRequest -UseBasicParsing -TimeoutSec 10 http://localhost:8080/v1/.well-known/ready
```

`plugin_daemon` 刚启动时可能因为 PostgreSQL 还在启动而重启一次。只要后面日志出现 `dify plugin db initialized`，并且容器保持 `Up`，就可以继续。

## 安装后端依赖并迁移数据库

```powershell
cd D:\demo1\dify\api
uv sync --group dev
uv run flask db upgrade
```

如果看到 `python-magic-bin` 的 warning，一般不影响本地启动。

## 启动 API

本机如果有全局环境变量 `LOG_FORMAT=json`，会覆盖 Dify 的 `.env`，导致 API 报错：

```text
ValueError: Invalid format 'json' for '%' style
```

启动 API 前显式覆盖日志格式：

```powershell
cd D:\demo1\dify\api

$env:LOG_OUTPUT_FORMAT='text'
$env:LOG_FORMAT='%(asctime)s,%(msecs)d %(levelname)-2s [%(filename)s:%(lineno)d] %(req_id)s %(message)s'
$env:ENABLE_COLLABORATION_MODE='false'

uv run flask run --host 0.0.0.0 --port=5001 --debug
```

验证：

```powershell
Invoke-WebRequest -UseBasicParsing -TimeoutSec 20 http://localhost:5001/health
```

正常返回示例：

```json
{"status":"ok","version":"1.14.2"}
```

## 启动 worker

Windows 下 Celery 建议使用 `-P solo`：

```powershell
cd D:\demo1\dify\api

$env:LOG_OUTPUT_FORMAT='text'
$env:LOG_FORMAT='%(asctime)s,%(msecs)d %(levelname)-2s [%(filename)s:%(lineno)d] %(req_id)s %(message)s'
$env:EDITION='SELF_HOSTED'

uv run celery -A app.celery worker -P solo -c 1 --loglevel INFO
```

日志里看到下面内容表示成功：

```text
Connected to redis://:**@localhost:6380/1
celery@... ready.
```

## 安装前端依赖

```powershell
cd D:\demo1\dify
fnm use 22
corepack enable
corepack prepare pnpm@11.1.1 --activate
pnpm install --frozen-lockfile
```

## 启动前端

Dify 1.14.2 使用 Next.js 16。Windows 下默认 Turbopack 首次编译 `/apps` 可能内存不足，报错：

```text
FATAL ERROR: Zone Allocation failed - process out of memory
```

推荐启动方式：

```powershell
cd D:\demo1\dify

fnm use 22
$env:NODE_OPTIONS='--max-old-space-size=8192'
$env:NEXT_TELEMETRY_DISABLED='1'

pnpm --dir web run dev -- --hostname 0.0.0.0 --port 3000
```

首次访问 `/apps` 或 `/install` 会触发编译，可能需要十几秒。后续访问会明显变快。

如果改用 webpack：

```powershell
pnpm --dir web exec next dev --webpack -H 0.0.0.0 -p 3000
```

但本版本 webpack 模式可能遇到 `loro-crdt` 的 `.wasm` 加载问题：

```text
The module seem to be a WebAssembly module, but module is not flagged as WebAssembly module for webpack.
```

所以优先使用 Turbopack 加大内存。

## 验证完整部署

```powershell
Invoke-WebRequest -UseBasicParsing -TimeoutSec 20 http://localhost:5001/health
Invoke-WebRequest -UseBasicParsing -TimeoutSec 60 http://localhost:3000
```

浏览器打开：

```text
http://localhost:3000
```

首次部署会跳转到：

```text
http://localhost:3000/install
```

页面应显示 `Setting up an admin account`，创建管理员账号即可开始使用。

## 常见问题

### Docker Desktop engine 停了

错误示例：

```text
failed to connect to the docker API at npipe:////./pipe/dockerDesktopLinuxEngine
```

处理：

```powershell
Start-Process -FilePath 'C:\Program Files\Docker\Docker\Docker Desktop.exe'
```

等待 Docker engine ready：

```powershell
docker info --format "READY {{.ServerVersion}} {{.OSType}}"
```

然后重启中间件：

```powershell
cd D:\demo1\dify\docker
docker compose --env-file middleware.env -f docker-compose.middleware.yaml -p dify up -d
```

### 80 端口被占用

如果只是二开，不建议使用完整 Docker Compose 的 nginx。前端本地跑 `3000`，API 本地跑 `5001` 即可。

### Redis 6379 被占用

本地已有 Redis 时，把 Dify Redis 宿主机端口改成 `6380`。对应修改：

- `api/.env` 的 `REDIS_PORT=6380`
- `api/.env` 的 `CELERY_BROKER_URL=redis://:difyai123456@localhost:6380/1`
- `docker/middleware.env` 的 `EXPOSE_REDIS_PORT=6380`

### API 日志格式报错

检查宿主环境变量：

```powershell
Get-ChildItem Env: | Where-Object { $_.Name -like 'LOG*' }
```

如果有 `LOG_FORMAT=json`，启动 API 和 worker 时按本文显式设置 `LOG_FORMAT`。

### 前端首次编译慢或 OOM

使用：

```powershell
$env:NODE_OPTIONS='--max-old-space-size=8192'
pnpm --dir web run dev -- --hostname 0.0.0.0 --port 3000
```

### 工作流页面 websocket error

本地源码二开方案只启动 Flask API，没有启动完整 Docker nginx 下的 Socket.IO 协同服务。如果 `ENABLE_COLLABORATION_MODE=true`，工作流页面会连接 `ws://localhost:5001/socket.io/`，Flask 会返回 404，浏览器里会出现：

```text
Console Error: websocket error
```

处理方式：在 `api/.env` 设置：

```env
ENABLE_COLLABORATION_MODE=false
```

然后重启 API。前端会通过 `/console/api/system-features` 拿到 `enable_collaboration_mode=false`，不再发起 websocket 连接。

## 停止服务

停止前端、API、worker：关闭对应终端即可。

停止中间件：

```powershell
cd D:\demo1\dify\docker
docker compose --env-file middleware.env -f docker-compose.middleware.yaml -p dify stop
```

删除容器但保留数据卷：

```powershell
docker compose --env-file middleware.env -f docker-compose.middleware.yaml -p dify down
```

谨慎：不要随便删除 `docker/volumes`，里面有本地 PostgreSQL、Redis、Weaviate 数据。

## 本地文件说明

这些文件通常不提交：

- `api/.env`
- `web/.env.local`
- `docker/middleware.env`
- `api/.venv`
- `node_modules`
- `web/.next`
- `docker/volumes`
- `.codex-logs`

二开代码改动正常提交。环境文件和数据卷按每台机器本地生成。
